# Environment Configuration
ENVIRONMENT=development
DEBUG=true

# Logging
LOG_LEVEL=INFO

# Storage Paths (basic structure only)
STORAGE_ROOT=./storage
DATABASE_STORAGE_PATH=./storage/databases
TEMP_STORAGE_PATH=./storage/temp

# Basic service communication timeout
SERVICE_TIMEOUT=30

# Docker Compose Overrides (optional)
# Uncomment and modify these if running with Docker
# Use service names instead of localhost when running in containers
# IMAGE_PROCESSING_SERVICE_URL=http://image-processing-service:8001
# VERIFICATION_SERVICE_URL=http://verification-service:8002
